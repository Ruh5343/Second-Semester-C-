#include <iostream>
using namespace std;

int sum(int n) {
    if (n == 0)     // base case
        return 0;
    else
        return n + sum(n - 1);   // recursive call
}

int main() {
    int n;
    cin >> n;

    cout << "Sum = " << sum(n) << endl;

    return 0;
}
