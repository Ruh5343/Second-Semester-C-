#include <iostream>
#include <string>
using namespace std;

int main() {
    string a, b;

    getline(cin, a);
    getline(cin, b);

    if(a == b)
        cout << "Both strings are equal";
    else if(a < b)
        cout << "String A comes before String B";
    else
        cout << "String A comes after String B";

    return 0;
}
