#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;

    for (int i = 1; i <= n; i++) {          // rows
        for (int j = 1; j <= i; j++) {      // print 1 to i
            cout << j << " ";
        }
        cout << endl;
    }

    return 0;
}
