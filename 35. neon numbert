#include <iostream>
using namespace std;
int main(){
int n,a,sum=0;
cin>>n;
a=n*n;
for(int i=a;i>0;i=i/10){
sum=sum+(i%10);
}
if(sum==n){
cout<<n<<" is a neon number";}
else{
cout<<n<<" is not a neon number";
}
return 0;
}
